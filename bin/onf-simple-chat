#!/usr/bin/env node

const cp = require('child_process');
const port = process.argv[2] || '3333';

const ps = cp.spawn('./bin/simple_chat', [port], {
    env: {
        LD_LIBRARY_PATH: './bin'
    }
});

ps.stderr.on('data', data => {
    console.log(`${data}`);
});

